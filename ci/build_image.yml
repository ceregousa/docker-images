---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: docker
    tag: dind

inputs:
  - name: pr

run:
  path: sh
  args:
    - -exc
    - |
      apk update && apk --no-cache add build-base

      cd pr && make
